# Suggested commands
- Current repo status: there is no `Cargo.toml` yet, so build/test commands are not runnable until the workspace is scaffolded.
- Repo inspection: `rg --files`, `rg -n "pattern" AGENTS.md ARCHITECTURE.md`, `find crates -maxdepth 3 -type f | sort`, `git status --short`.
- Expected Rust workspace commands once scaffolded:
  - `cargo fmt --all`
  - `cargo clippy --workspace --all-targets`
  - `cargo test --workspace`
  - `cargo run -p glossa-bin -- daemon --config /path/to/config.toml`
  - `cargo run -p glossa-bin -- ctl toggle`
  - `cargo run -p glossa-bin -- doctor --config /path/to/config.toml`
  - `cargo run -p glossa-bin -- status`
- Expected user-session diagnostics once a systemd unit exists:
  - `systemctl --user status glossa.service`
  - `journalctl --user -u glossa.service -f`
- Linux utility commands commonly used in this repo/session: `git status`, `git diff`, `rg`, `find`, `ls`, `pwd`.
